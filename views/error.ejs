<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Error</title>
</head>
<body>

  <div style="margin: 50px auto; max-width: 600px;">
    <h2>Something went wrong</h2>

    <h4>
      Error <%= (err && err.statusCode) ? err.statusCode : 500 %>
    </h4>

    <p>
      <%= (typeof message === "string" && message.length > 0)
          ? message
          : "An unexpected error occurred." %>
    </p>

    <a href="/">Go back home</a>
  </div>

</body>
</html>
